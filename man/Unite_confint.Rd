% Generated by roxygen2: do not edit by hand
% Please edit documentation in R/Unite.R
\name{Unite_confint}
\alias{Unite_confint}
\title{Unite Columns Confint in a Data Frame}
\usage{
Unite_confint(
  data,
  col1,
  col2,
  Col = NULL,
  sep = ", ",
  remove = TRUE,
  left = "[",
  right = "]"
)
}
\arguments{
\item{data}{A data frame.}

\item{col1}{The first column to unite. Can be numeric (column index) or character (column name).}

\item{col2}{The second column to unite. Can be numeric (column index) or character (column name).}

\item{Col}{Optional, name for the new column (default = NULL).}

\item{sep}{Separator string to use between the combined values (default = ",").}

\item{remove}{Logical, whether to remove the original columns after uniting (default = TRUE).}

\item{left}{String to add to the left side of the combined values (default = "[").}

\item{right}{String to add to the right side of the combined values (default = "]").}
}
\value{
A data frame with the combined column.
}
\description{
Unite Columns Confint in a Data Frame
}
\examples{
\dontrun{
# Example data
data <- data.frame(
  block = c("IMAG", "IMAG", "IMAG", "IMAG"),
  name = c("imag1", "imag2", "imag3", "imag4"),
  Original = c("0.7093265 ***", "0.8773078 ***", "0.8417485 ***", "0.5691934 ***"),
  sig = c("***", "***", "***", "***"),
  Mean.Boot = c(0.7088215, 0.8750787, 0.8401757, 0.5647333),
  Std.Error = c(0.05949724, 0.02402834, 0.02970117, 0.07686419),
  perc.025 = c(0.5774529, 0.8231642, 0.7784983, 0.3876417),
  perc.975 = c(0.8105249, 0.9146114, 0.8863100, 0.6961572)
)

# Unite using column numbers
result1 <- Unite_confint(data, 3, 4)
print(result1)

# Unite using column numbers with a new column name
result2 <- Unite_confint(data, 3, 4, "New")
print(result2)

# Unite using column names
result3 <- Unite_confint(data, "Original", "sig")
print(result3)

# Unite using column names with a new column name
result4 <- Unite_confint(data, "Original", "sig", "New")
print(result4)
}
}
