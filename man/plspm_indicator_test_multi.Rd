% Generated by roxygen2: do not edit by hand
% Please edit documentation in R/plspm_indicator_test_multi.R
\name{plspm_indicator_test_multi}
\alias{plspm_indicator_test_multi}
\title{Perform Bootstrap and Permutation Tests for PLS-PM Indicators Across Groups}
\usage{
plspm_indicator_test_multi(
  Data,
  path_matrix,
  blocks,
  grp,
  n_perm = 50,
  n_boot = 50,
  n_cores = 12,
  grp_select = NULL
)
}
\arguments{
\item{Data}{A data frame containing the dataset to be analyzed.}

\item{path_matrix}{A square matrix defining the path relationships among constructs in the PLS-PM model.}

\item{blocks}{A list defining the measurement blocks (i.e., sets of observed variables) for each construct.}

\item{grp}{A string specifying the name of the grouping variable in `Data`.}

\item{n_perm}{Number of permutation tests to perform. Default is 50.}

\item{n_boot}{Number of bootstrap resamples to perform. Default is 50.}

\item{n_cores}{Number of cores to use for parallel processing. Default is 12.}

\item{grp_select}{A character vector of length 2 specifying the group pair to compare. Default is NULL (compares all pairs).}
}
\value{
A tibble containing the test results for all indicators, including estimates, standard errors, p-values, and comparisons.
The returned tibble includes the following columns:
- `indicator`: The indicator being tested.
- `g1_est`: The estimate for the first group.
- `g2_est`: The estimate for the second group.
- `g1_se`: The standard error for the first group.
- `g2_se`: The standard error for the second group.
- `se_diff`: The difference in standard errors between the groups.
- `p_value`: The p-value for the test.
- `invariance`: Whether the indicator is invariant between the groups (`"OK"` or `"ns"`).
- `comparison`: The comparison between the two groups (e.g., "group1_vs_group2").
}
\description{
This function performs bootstrap and permutation tests for PLS-PM indicators across
selected group pairs or all possible group pairs in the dataset. The results include
group-specific estimates, standard errors, p-values, and invariance checks.
}
\examples{
\dontrun{
result <- plspm_indicator_test_multi(
  Data = ed_tam2_st,
  path_matrix = edmodel,
  blocks = edblock,
  grp = "Class",
  grp_select = c("1", "3"),
  n_perm = 100,
  n_boot = 50,
  n_cores = 13
)
print(result)
}
}
