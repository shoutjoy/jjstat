% Generated by roxygen2: do not edit by hand
% Please edit documentation in R/lpa_bic_plot.R
\name{lpa_BIC_plot}
\alias{lpa_BIC_plot}
\title{Plot BIC Values for Latent Profile Analysis using mclust}
\usage{
lpa_BIC_plot(
  data,
  n_profiles_range = 1:6,
  type = "all",
  legend_position = "top",
  covar_model = c("EII", "EEE", "VII", "EEV", "EEI", "VVI", "VVV"),
  set_n_profile = NULL,
  xintercept = 4,
  size.text = 4,
  flip = TRUE,
  na2zero = FALSE,
  new.window = TRUE,
  basic = TRUE
)
}
\arguments{
\item{data}{A raw data frame (e.g., survey or psychological item data) or a BIC summary table.}

\item{n_profiles_range}{A numeric vector indicating the number of profiles to evaluate. Default is 1:6.}

\item{type}{The output type: one of "all", "graph", "data", "long_data", "wide_data". Default is "all".}

\item{legend_position}{Position of the legend in the plot. Default is "top".}

\item{covar_model}{A vector of covariance structures to use (e.g., "EII", "EEE", ...). Default includes all.}

\item{set_n_profile}{Optional: specify number of profiles to highlight with a vertical line.}

\item{xintercept}{Default x-position for vertical line (dashed).}

\item{size.text}{Size of text in plot labels. Default is 4.}

\item{flip}{Whether to multiply BIC values by -1 for plotting (to make larger = better). Default is TRUE.}

\item{na2zero}{Not used yet (reserved for missing value handling).}

\item{new.window}{Whether to plot in a new window (only for local use). Default is TRUE.}

\item{basic}{Whether to restrict to basic models (EII, EEE, VII, EEI). Default is TRUE.}
}
\value{
Depending on `type`, returns a plot object, data table, or list of results.
}
\description{
This function fits latent profile models using the `mclust` package
and plots BIC values across profile numbers and covariance structures.
If a raw data frame is passed, it computes the BIC matrix automatically.
}
\examples{
# Example using built-in mtcars dataset
data <- mtcars[, c("mpg", "disp", "hp", "wt", "qsec")]
scaled_data <- scale(data)
result <- lpa_BIC_plot(as.data.frame(scaled_data))
# 데이터 준비: mtcars에서 LPA에 적합한 일부 변수만 추출
lpa_test_data <- mtcars \%>\%
  dplyr::select(mpg, disp, hp, wt, qsec) \%>\%
  scale() \%>\%         # 정규화
  as.data.frame()
# 기본 실행: BIC plot 출력
lpa_BIC_plot(lpa_test_data)


}
