% Generated by roxygen2: do not edit by hand
% Please edit documentation in R/ave_candidates_plot.R
\name{ave_candidates_plot}
\alias{ave_candidates_plot}
\title{ave_candidates_plot: 문항 제거 전후 AVE 비교 시각화}
\usage{
ave_candidates_plot(
  df,
  latent = NULL,
  range_focus = TRUE,
  font_size = 4,
  padding = 0.04,
  v_line_show = TRUE
)
}
\arguments{
\item{df}{lavaan 객체 또는 ave_raise_candidates() 결과}

\item{latent}{특정 잠재변수만 시각화 (선택)}

\item{range_focus}{TRUE면 AVE 변화 중심으로 x축 범위 조정}

\item{font_size}{텍스트 크기}

\item{padding}{범위조절 값 (기본 0.04)}

\item{v_line_show}{각 문항의 ave_current 위치에 수직선 추가 여부}
}
\value{
ggplot2 객체
}
\description{
ave_candidates_plot: 문항 제거 전후 AVE 비교 시각화
}
\examples{
\dontrun{
library(lavaan)
library(ggplot2)
library(dplyr)

# 예시 데이터: HolzingerSwineford1939
model <- '
  visual  =~ x1 + x2 + x3
  textual =~ x4 + x5 + x6
  speed   =~ x7 + x8 + x9
'
fit <- cfa(model, data = lavaan::HolzingerSwineford1939)

# 1. lavaan 객체 입력 시 자동 처리
ave_candidates_plot(fit)

# 2. 특정 잠재변수만 보기
ave_candidates_plot(fit, latent = "visual")

# 3. AVE 변화량이 두드러진 경우에만 확대해서 보기
ave_candidates_plot(fit, range_focus = TRUE)

# 4. AVE 변화 전후 수직선 제거
ave_candidates_plot(fit, v_line_show = FALSE)

# 5. font 크기 조절
ave_candidates_plot(fit, font_size = 3)

# 6. ave_raise_candidates() 결과를 직접 전달
result <- ave_raise_candidates(fit, print_result = FALSE)
ave_candidates_plot(result)
}
}
