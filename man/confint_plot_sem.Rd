% Generated by roxygen2: do not edit by hand
% Please edit documentation in R/confint_plot_sem.R
\name{confint_plot_sem}
\alias{confint_plot_sem}
\title{Plot Confidence Intervals for SEM Paths using lavaan}
\usage{
confint_plot_sem(
  lavaan_obj,
  type = "g",
  effect = "~",
  var_named = NULL,
  size_axis = 14,
  size_text = 4,
  linewidth = 2
)
}
\arguments{
\item{lavaan_obj}{A fitted `lavaan` object (e.g., from `sem()` or `cfa()`).}

\item{type}{Type of output: `"g"` for the ggplot object, `"res"` for the result table, `"all"` for both.}

\item{effect}{Operator type to filter by, default is `"~"` for regression paths. Use `"=~"` for latent loadings.}

\item{var_named}{Named vector for renaming variables using `replace_df_rep()`. Default is `NULL`.}

\item{size_axis}{Numeric. Font size for axis labels in the plot. Default is 14.}

\item{size_text}{Numeric. Size of text showing estimates in the plot. Default is 4.}

\item{linewidth}{Numeric. Width of confidence interval error bars. Default is 2.}
}
\value{
A ggplot object, a data frame, or a list depending on the `type` argument.
}
\description{
Create a confidence interval plot of structural paths from a lavaan SEM object.
}
\examples{
\dontrun{
library(lavaan)
model <- '
  visual  =~ x1 + x2 + x3
  textual =~ x4 + x5 + x6
  speed   =~ x7 + x8 + x9
  speed ~ visual + textual
'
fit <- sem(model, data = HolzingerSwineford1939)
named_vec <- c("visual" = "시각", "textual" = "언어", "speed" = "속도")
confint_plot_sem(fit, type = "g", effect = "~", var_named = named_vec)
}
}
