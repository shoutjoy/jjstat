% Generated by roxygen2: do not edit by hand
% Please edit documentation in R/rows_to_colnames.R
\name{rows_to_colnames}
\alias{rows_to_colnames}
\title{Convert a Row into Column Names of a Data Frame}
\usage{
rows_to_colnames(df, row = 1)
}
\arguments{
\item{df}{A data frame. The input data where one row will be promoted to column names.}

\item{row}{An integer indicating the row number to use as column names. Defaults to 1.}
}
\value{
A data frame with updated column names and the specified row removed.
}
\description{
This function sets the values of a specified row in a data frame as the new column names.
The row used for column names is then removed from the data.
}
\examples{
# Create a simple data frame
df <- data.frame(
  V1 = c("Name", "Alice", "Bob"),
  V2 = c("Age", 25, 30),
  V3 = c("Gender", "F", "M"),
  stringsAsFactors = FALSE
)

# Convert the first row to column names
new_df <- rows_to_colnames(df)
print(new_df)

# Use the second row instead
df2 <- data.frame(
  A = c("Header1", "X", "Y"),
  B = c("Header2", 1, 2),
  stringsAsFactors = FALSE
)
rows_to_colnames(df2, row = 2)
}
