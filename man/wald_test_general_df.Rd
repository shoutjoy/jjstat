% Generated by roxygen2: do not edit by hand
% Please edit documentation in R/wald_test+general_df.R
\name{wald_test_general_df}
\alias{wald_test_general_df}
\title{Perform Wald Tests for Multiple Hypotheses}
\usage{
wald_test_general_df(
  model,
  ...,
  rhs = 0,
  test = c("F", "Chisq"),
  vcov_func = NULL,
  full = FALSE
)
}
\arguments{
\item{model}{A fitted model object, such as an object from `lm` or `glm`.}

\item{...}{Hypotheses to test. Each hypothesis can be specified as a character string
(e.g., `"x1 - x2 + x3 = 0"`) or a numeric vector/matrix (e.g., `c(1, -1, 1)`).}

\item{rhs}{Right-hand side of the hypothesis equations. Default is `0`.}

\item{test}{A character string specifying the type of test to perform. Options are `"F"`
(default) or `"Chisq"`.}

\item{vcov_func}{An optional function for specifying a custom covariance matrix. Default is `NULL`.}

\item{full}{Logical. If `TRUE`, returns a tibble with full results. Default is `FALSE`.}
}
\value{
A tibble with results for all tested hypotheses. Each row corresponds to one hypothesis.
}
\description{
This function allows multiple hypotheses to be tested on a fitted model. It internally
uses `wald_test_general` to perform each test and combines the results into a single
tibble.
}
\examples{
# Example: Multiple hypotheses with a linear model
set.seed(123)
data <- data.frame(x1 = rnorm(100), x2 = rnorm(100), x3 = rnorm(100), y = rnorm(100))
model <- lm(y ~ x1 + x2 + x3, data = data)

# Perform multiple Wald tests
result <- wald_test_general_df(model, "x1 - x2 + x3 = 0", "x1 - x2 = 0", c(1, -1, 1), test = "F", full = TRUE)
print(result)

}
